import template from './ngChart.html';
import controller from './ngChart.controller';

let ngChartComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default ngChartComponent;
